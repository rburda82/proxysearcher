<UserControl x:Class="ProxySearch.Console.Controls.DataGridHelpControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="500" x:Name="HelpControl">
    <TextBlock Text="{Binding ElementName=HelpControl, Path=Text}">
        <TextBlock.Style>
            <Style TargetType="{x:Type TextBlock}" BasedOn="{StaticResource HelpText}" />
        </TextBlock.Style>
    </TextBlock>
</UserControl>
