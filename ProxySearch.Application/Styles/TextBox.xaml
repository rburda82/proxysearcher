<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Common.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource ControlStyle}" />

    <Style x:Key="DataGridTextBox" TargetType="{x:Type TextBox}" BasedOn="{StaticResource ControlStyle}">
        <Setter Property="IsReadOnly" Value="True" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
    </Style>

    <Style x:Key="ReadOnlyTextBox" TargetType="{x:Type TextBox}" BasedOn="{StaticResource ControlStyle}">
        <Setter Property="IsReadOnly" Value="True" />
        <Setter Property="Background" Value="#1F000000" />
    </Style>
</ResourceDictionary>