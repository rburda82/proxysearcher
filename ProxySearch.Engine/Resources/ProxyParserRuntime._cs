using System.Net;
using System.Text.RegularExpressions;
using ProxySearch.Engine.Proxies;
using System.Web;
using System.Collections.Generic;
using System;
using System.Linq;

namespace ProxySearch.Engine.Resources
{{
    public class ProxyParserRuntime
    {{
        public Proxy GetProxyOrNull(string document, Match match)
        {{
            try
            {{
                 {0}
            }}
            catch
            {{
                return null;
            }}
        }}
    }}
}}
